modules:
  http_stuff:
    # The protocol over which the probe will take place (http, tcp, dns, icmp).
    prober: http

    # How long the probe will wait before giving up.
    timeout: 30s

    http:
      # Accepted status codes for this probe. Defaults to 2xx.
      #[ valid_status_codes: <int>, ... | default = 2xx ]  # yamllint disable-line rule:comments

      # Accepted HTTP versions for this probe.
      #[ valid_http_versions: <string>, ... ]  # yamllint disable-line rule:comments

      # The HTTP method the probe will use.
      #[ method: <string> | default = "GET" ]  # yamllint disable-line rule:comments

      # The HTTP headers set for the probe.
      #headers:  # yamllint disable-line rule:comments
      #  [ <string>: <string> ... ]

      # Whether or not the probe will follow any redirects.
      #[ no_follow_redirects: <boolean> | default = false ]  # yamllint disable-line rule:comments

      # Probe fails if SSL is present.
      #[ fail_if_ssl: <boolean> | default = false ]  # yamllint disable-line rule:comments

      # Probe fails if SSL is not present.
      #[ fail_if_not_ssl: <boolean> | default = false ]  # yamllint disable-line rule:comments

      # Probe fails if response body matches regex.
      #fail_if_body_matches_regexp:  # yamllint disable-line rule:comments
      #  [ - <regex>, ... ]

      # Probe fails if response body does not match regex.
      #fail_if_body_not_matches_regexp:  # yamllint disable-line rule:comments
      #  [ - <regex>, ... ]

      # Probe fails if response header matches regex. For headers with multiple values, fails if *at least one* matches.  # yamllint disable-line rule:line-length
      #fail_if_header_matches:  # yamllint disable-line rule:comments
      #  [ - <http_header_match_spec>, ... ]

      # Probe fails if response header does not match regex. For headers with multiple values, fails if *none* match.  # yamllint disable-line rule:line-length
      #fail_if_header_not_matches:  # yamllint disable-line rule:comments
      #  [ - <http_header_match_spec>, ... ]

      # Configuration for TLS protocol of HTTP probe.
      #tls_config:  # yamllint disable-line rule:comments
      #  [ <tls_config> ]

      # The HTTP basic authentication credentials for the targets.
      #basic_auth:  # yamllint disable-line rule:comments
      #  [ username: <string> ]
      #  [ password: <secret> ]

      # The bearer token for the targets.
      #[ bearer_token: <secret> ]  # yamllint disable-line rule:comments

      # The bearer token file for the targets.
      #[ bearer_token_file: <filename> ]  # yamllint disable-line rule:comments

      # HTTP proxy server to use to connect to the targets.
      #[ proxy_url: <string> ]  # yamllint disable-line rule:comments

      # The IP protocol of the HTTP probe (ip4, ip6).
      preferred_ip_protocol: "ip4"  # defaults to "ip6"
      ip_protocol_fallback: false  # no fallback to "ip6"

      # The body of the HTTP request used in probe.
      #body: [ <string> ]  # yamllint disable-line rule:comments
