docker cp to backup dbs (ciao, statping, prometheus)

(check if prometheus has a sqlite, actually)

put the prometheus.yml and the
  gf-provisioning/notifiers/conf.yml after envsubst

Also the logstash conf

take main dashboard from monitor1

https://grafana.com/grafana/dashboards/4271

(future) aggregate all metrics of apis in one endpoint (a specific rewrite url rule in traefik label wont work)
(future) aggregate all statistics of apis in one endpoint
