// This is a minimal test that just requires the core module. It is useful
// for getting any coverage >0%, which is necessary for coverage results to
// be uploaded to coveralls.io at all.
var core = require('../lib/core');
