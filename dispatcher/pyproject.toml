[tool.poetry]
name = "dispatcher"
version = "0.1.0"
description = "Polls rabbitMQ and takes action"
authors = ["linuxbandit <bellicaf@tcd.ie>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pika = "^1.3.2"
jinja2 = "^3.1.6"


[tool.poetry.group.dev.dependencies]
faker = "^37.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
